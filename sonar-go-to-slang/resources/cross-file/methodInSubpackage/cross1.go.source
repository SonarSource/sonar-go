package methodInSubpackage

import (
	"example.com/met_in_sub_pack/foo"
	"example.com/met_in_sub_pack/foo/bar"
	"fmt"
)

func Cross1() {
	a := foo.Calculate(bar.MagicNumber)
	fmt.Print(a)
	my := new(foo.MyType)
	my.SomePublicMethod()
	bar.BarMethod()
}
